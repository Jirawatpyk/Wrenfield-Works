# Payload CMS resolves its deep subpath exports (@payloadcms/next/views, etc.)
# through Next's bundler; pnpm's default isolated symlinks break that resolution.
# A hoisted (flat) node_modules layout — like npm — fixes "Module not found".
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
